<h1> <%= @heroine.super_name %> </h1>

<h2>Real Name: <%= @heroine.name %> </h2>

<h2>Powers:</h2>
<p>
    <% @heroine.powers.each do |power|%>
        <li>
            <%= link_to power.name, power_path(power) %> strength: 
        </li>
    <% end %>
</p>